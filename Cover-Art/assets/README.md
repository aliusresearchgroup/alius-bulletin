# Shared Cover Artwork Assets

This directory contains the only approved committed PDF asset in the repository:

- `front-cover-empty-no-leaf.pdf` — shared ALIUS cover background substrate.

It also contains the canonical editable vector leaf:

- `alius-leaf.svg` — canonical source for `Cover-Art/generated/alius-leaf-paths.tex`, which is generated by `Cover-Art/generate_leaf_cache.py` and used by `Cover-Art/cover-style.tex`.

No article, interview, guideline, issue, or QA PDF should be committed elsewhere in the repository.
